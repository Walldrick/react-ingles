const VideoSection = () => {
    return (
        <section className="border border-amber-50 rounded-lg">
            <video controls={}>

            </video>
        </section>
    );
}

export default VideoSection;